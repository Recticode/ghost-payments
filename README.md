# Ghost Payments

We’ve had reports that orders are being marked as paid even when the payment fails. This seems to happen inconsistently.

Fix the bug!

Run `recticode start ghost-payments` in your terminal to start the challenge.